require "spec_helper"

describe ScanlibsParse do
  xit "has a version number" do
    expect(ScanlibsParse::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(false).to eq(true)
  end
end
